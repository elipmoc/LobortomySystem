<%= image_tag(@image.image_name,width: '50%') %>
  <%= button_to "編集", {action: "edit", id: @image.id}, {method: :get} %>
    ファイル名：
    <%= @image.image_name %>
      <br> セリフ：
      <%= @image.serif_name %>
        <br> セリフ読みがな：
        <%= @image.serif_ruby_name %>
          <br> 登場キャラ：
          <% @image.chara_mappings.each do |chara_mapping| %>
            <%= chara_mapping.chara.chara_name%>
              ,

              <% end %>